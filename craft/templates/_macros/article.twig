{#
 # A simple macro to loop the blocks in our given article
 # field and assign them a template
 #}
{% macro loop(field) %}

  {% if field | length %}

    {% for block in field %}

      {% include '_includes/article/'~block.type %}

    {% endfor %}

  {% endif %}

{% endmacro %}
